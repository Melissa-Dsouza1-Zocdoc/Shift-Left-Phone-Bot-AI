# Miscellaneous Tests - Part C-PatientAndTools - Unit Test Mapping

> Repo: [Zocdoc/zo-agent](https://github.com/Zocdoc/zo-agent)
> Folder: `tests/` (root)
> Generated: 2026-05-07
> Stack: Python / pytest / LiveKit Agents

## test_patient_auth.py

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|---|---|---|---|---|---|
| 1 |  | `test_get_patient_found` | Test successful patient lookup. | Calls get_patient. Asserts 5 conditions on result. | Get patient found | In scope: unit-level behavior. Out of scope: integration with live services. |
| 2 |  | `test_get_patient_not_found_wrong_dob` | Test patient not found due to wrong DOB. | Calls get_patient. Asserts 2 conditions on result. | Get patient not found wrong dob | In scope: unit-level behavior. Out of scope: integration with live services. |
| 3 |  | `test_get_patient_not_found_no_match` | Test patient not found - name doesn't exist. | Calls get_patient. Asserts 2 conditions on result. | Get patient not found no match | In scope: unit-level behavior. Out of scope: integration with live services. |
| 4 |  | `test_get_lab_results_negative` | Test getting negative COVID result. | Calls get_lab_results, result.lower. Asserts 'negative' in result.lower(). | Get lab results negative | In scope: unit-level behavior. Out of scope: integration with live services. |
| 5 |  | `test_get_lab_results_positive` | Test getting positive COVID result. | Calls get_lab_results, result.lower. Asserts 'positive' in result.lower(). | Get lab results positive | In scope: unit-level behavior. Out of scope: integration with live services. |
| 6 |  | `test_get_lab_results_not_found` | Test lab results when patient not found. | Calls get_lab_results, result.lower. Asserts 'unable to find' in result.lower(). | Get lab results not found | In scope: unit-level behavior. Out of scope: integration with live services. |

### Suggested Missing Tests
- **Locked-out patient** - After N failed attempts, lock out and transfer.
- **Audit log on failed auth** - Each failed auth must produce an audit entry without PHI.

### Improvement Suggestions / Irrelevant Tests
- **Limited matrix** - Add parametrize for combinations of name/DOB/phone match levels.

## test_patient_data.py

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|---|---|---|---|---|---|
| 7 |  | `test_lookup_patient_found` | Test looking up a patient that exists. | Calls lookup_patient. Asserts 6 conditions on result. | Lookup patient found | In scope: unit-level behavior. Out of scope: integration with live services. |
| 8 |  | `test_lookup_patient_not_found` | Test looking up a patient who should not be found. | Calls lookup_patient. Asserts profile is None. | Lookup patient not found | In scope: unit-level behavior. Out of scope: integration with live services. |
| 9 |  | `test_lookup_patient_lowercase` | Test looking up patient with lowercase names (case-insensitive). | Calls lookup_patient. Asserts 4 conditions on result. | Lookup patient lowercase | In scope: unit-level behavior. Out of scope: integration with live services. |
| 10 |  | `test_lookup_patient_uppercase` | Test looking up patient with uppercase names (case-insensitive). | Calls lookup_patient. Asserts 2 conditions on result. | Lookup patient uppercase | In scope: unit-level behavior. Out of scope: integration with live services. |
| 11 |  | `test_lookup_patient_mixed_case` | Test looking up patient with mixed case names. | Calls lookup_patient. Asserts 2 conditions on result. | Lookup patient mixed case | In scope: unit-level behavior. Out of scope: integration with live services. |
| 12 |  | `test_verify_patient_correct_credentials` | Test verifying patient with correct DOB. | Calls lookup_patient, verify_patient. Asserts verify_patient(profile, '1985-08-23') is True. | Verify patient correct credentials | In scope: unit-level behavior. Out of scope: integration with live services. |
| 13 |  | `test_verify_patient_wrong_credentials` | Test verifying patient with wrong DOB. | Calls lookup_patient, verify_patient. Asserts verify_patient(profile, '1990-01-01') is False. | Verify patient wrong credentials | In scope: unit-level behavior. Out of scope: integration with live services. |
| 14 |  | `test_get_available_slots` | Test getting available time slots. | Calls get_available_slots. Asserts 4 conditions on result. | Get available slots | In scope: unit-level behavior. Out of scope: integration with live services. |
| 15 |  | `test_get_available_slots_empty` | Test getting available slots for patient with no slots. | Calls get_available_slots. Asserts 2 conditions on result. | Get available slots empty | In scope: unit-level behavior. Out of scope: integration with live services. |
| 16 |  | `test_get_patient_success_lowercase` | Test successful authentication with lowercase names. | Calls get_patient. Asserts 4 conditions on result. | Get patient success lowercase | In scope: unit-level behavior. Out of scope: integration with live services. |
| 17 |  | `test_get_patient_all_cases` | Test that all patients can be found with various case combinations. | Calls get_patient. Asserts 2 conditions on result. | Get patient all cases | In scope: unit-level behavior. Out of scope: integration with live services. |

### Suggested Missing Tests
- **Field-level redaction** - Verify each PHI field is redacted in the same way (no inconsistencies).
- **Schema evolution** - Backwards compatibility when zotools adds a new field.

### Improvement Suggestions / Irrelevant Tests
- **Pydantic version pinning** - Validate against the pydantic v2 strictness settings.

## test_practice_info.py

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|---|---|---|---|---|---|
| 18 |  | `TestProviderSpokenName.test_ipa_with_prefix_edge_cases` | IPA substitution combined with credential-based prefix and spacing edge cases. | Fixtures: full_name, first_name, last_name, title, first_name_ipa. Calls _make_provider. Asserts p.spoken_name == expected. | Ipa with prefix edge cases (parametrized) | In scope: TestProviderSpokenName. Out of scope: integration with live services. |
| 19 |  | `TestProviderSpokenName.test_doctor_prefix_from_credentials` | Doctor prefix logic based on credentials and existing full_name prefix (no IPA). | Fixtures: title, full_name, expected. Calls _make_provider. Asserts p.spoken_name == expected. | Doctor prefix from credentials (parametrized) | In scope: TestProviderSpokenName. Out of scope: integration with live services. |
| 20 |  | `TestProviderSpokenName.test_empty_or_whitespace_ipa_treated_as_no_ipa` | Empty-string or whitespace-only IPA fields should be treated like None. | Fixtures: first_name_ipa, last_name_ipa. Calls _make_provider. Asserts p.spoken_name == 'Doctor Jane Smith'. | Empty or whitespace ipa treated as no ipa (parametrized) | In scope: TestProviderSpokenName. Out of scope: integration with live services. |
| 21 |  | `TestPatientInfoRequirementLevel.test_parses_all_values` | Parses all values | Calls PatientInfoRequirementLevel. Asserts 3 conditions on result. | Parses all values | In scope: TestPatientInfoRequirementLevel. Out of scope: integration with live services. |
| 22 |  | `TestPatientInfoRequirementLevel.test_rejects_invalid_value` | Rejects invalid value | Calls pytest.raises, PatientInfoRequirementLevel. Verifies behavior via mocks/return value. | Rejects invalid value | In scope: TestPatientInfoRequirementLevel. Out of scope: real LLM/network calls (mocked), no explicit assertions. |
| 23 |  | `TestPatientInfoRequirement.test_defaults_to_not_asked` | Defaults to not asked | Calls PatientInfoRequirement. Asserts 2 conditions on result. | Defaults to not asked | In scope: TestPatientInfoRequirement. Out of scope: integration with live services. |
| 24 |  | `TestPatientInfoRequirement.test_parses_from_dict` | Parses from dict | Calls PatientInfoRequirement. Asserts 2 conditions on result. | Parses from dict | In scope: TestPatientInfoRequirement. Out of scope: integration with live services. |
| 25 |  | `TestPatientInformationSettings.test_defaults_all_not_asked` | Defaults all not asked | Calls PatientInformationSettings. Asserts 3 conditions on result. | Defaults all not asked | In scope: TestPatientInformationSettings. Out of scope: integration with live services. |
| 26 |  | `TestPatientInformationSettings.test_parses_from_dict` | Parses from dict | Calls PatientInformationSettings. Asserts 4 conditions on result. | Parses from dict | In scope: TestPatientInformationSettings. Out of scope: integration with live services. |
| 27 |  | `TestPracticeInfoPatientInformation.test_patient_information_optional` | Patient information optional | Calls PracticeInfo. Asserts info.patient_information is None. | Patient information optional | In scope: TestPracticeInfoPatientInformation. Out of scope: integration with live services. |
| 28 |  | `TestPracticeInfoPatientInformation.test_patient_information_parsed` | Patient information parsed | Calls PracticeInfo. Asserts 2 conditions on result. | Patient information parsed | In scope: TestPracticeInfoPatientInformation. Out of scope: integration with live services. |

### Suggested Missing Tests
- **Stale cache TTL** - Practice info cached — assert refresh after TTL.
- **Cache miss falls through to remote** - Verify fallback on cache miss.

### Improvement Suggestions / Irrelevant Tests
- **Mocked remote** - Use real-shaped JSON fixtures captured from zotools.

## test_practice_info_schema.py

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|---|---|---|---|---|---|
| 29 |  | `TestEnums.test_insurance_network_type_values` | Test InsuranceNetworkType enum values. | Asserts 4 conditions on result. | Insurance network type values | In scope: TestEnums. Out of scope: integration with live services. |
| 30 |  | `TestEnums.test_transfer_number_type_values` | Test TransferNumberType enum values. | Asserts 4 conditions on result. | Transfer number type values | In scope: TestEnums. Out of scope: integration with live services. |
| 31 |  | `TestEnums.test_language_type_values` | Test LanguageType enum values. | Asserts LanguageType.ES == 'es'. | Language type values | In scope: TestEnums. Out of scope: integration with live services. |
| 32 |  | `TestEnums.test_preflight_message_values` | Test PreflightMessage enum values. | Asserts 8 conditions on result. | Preflight message values | In scope: TestEnums. Out of scope: integration with live services. |
| 33 |  | `TestProvider.test_provider_required_fields` | Test Provider with all required fields. | Calls Provider. Asserts 3 conditions on result. | Provider required fields | In scope: TestProvider. Out of scope: integration with live services. |
| 34 |  | `TestProvider.test_provider_with_optional_fields` | Test Provider with optional fields. | Calls Provider. Asserts provider.is_facility is True. | Provider with optional fields | In scope: TestProvider. Out of scope: integration with live services. |
| 35 |  | `TestProvider.test_provider_missing_required_field` | Test Provider validation fails without required fields. | Calls pytest.raises, Provider. Asserts 'first_name' in str(exc_info.value). | Provider missing required field | In scope: TestProvider. Out of scope: integration with live services. |
| 36 |  | `TestLocation.test_location_required_fields` | Test Location with all required fields. | Calls Location. Asserts 3 conditions on result. | Location required fields | In scope: TestLocation. Out of scope: integration with live services. |
| 37 |  | `TestLocation.test_location_with_optional_fields` | Test Location with optional fields. | Calls Location. Asserts 4 conditions on result. | Location with optional fields | In scope: TestLocation. Out of scope: integration with live services. |
| 38 |  | `TestLocation.test_location_with_provider_locations` | Test Location with nested provider locations. | Calls Location, ProviderLocation. Asserts 2 conditions on result. | Location with provider locations | In scope: TestLocation. Out of scope: integration with live services. |
| 39 |  | `TestTransferNumber.test_transfer_number_required_fields` | Test TransferNumber with required fields. | Calls TransferNumber. Asserts 2 conditions on result. | Transfer number required fields | In scope: TestTransferNumber. Out of scope: integration with live services. |
| 40 |  | `TestTransferNumber.test_transfer_number_with_optional_fields` | Test TransferNumber with optional fields. | Calls TransferNumber. Asserts 2 conditions on result. | Transfer number with optional fields | In scope: TestTransferNumber. Out of scope: integration with live services. |
| 41 |  | `TestTransferNumber.test_transfer_number_enum_from_string` | Test TransferNumber parses enum from string. | Calls TransferNumber. Asserts tn.transfer_number_type == TransferNumberType.BILLING. | Transfer number enum from string | In scope: TestTransferNumber. Out of scope: integration with live services. |
| 42 |  | `TestVisitReason.test_visit_reason_required_fields` | Test VisitReason with required fields. | Calls VisitReason. Asserts 3 conditions on result. | Visit reason required fields | In scope: TestVisitReason. Out of scope: integration with live services. |
| 43 |  | `TestVisitReason.test_visit_reason_with_specialties` | Test VisitReason with specialties. | Calls VisitReason, Specialty. Asserts 2 conditions on result. | Visit reason with specialties | In scope: TestVisitReason. Out of scope: integration with live services. |
| 44 |  | `TestPracticeInfo.test_parse_valid_response` | Test parsing a complete valid API response. | Fixtures: valid_practice_info_data. Calls PracticeInfo. Asserts 5 conditions on result. | Parse valid response | In scope: TestPracticeInfo. Out of scope: integration with live services. |
| 45 |  | `TestPracticeInfo.test_providers_parsed` | Test that providers are parsed correctly. | Fixtures: valid_practice_info_data. Calls PracticeInfo. Asserts 3 conditions on result. | Providers parsed | In scope: TestPracticeInfo. Out of scope: integration with live services. |
| 46 |  | `TestPracticeInfo.test_allowed_networks_parsed_as_enums` | Test that allowed networks are parsed as enums. | Fixtures: valid_practice_info_data. Calls PracticeInfo. Asserts 3 conditions on result. | Allowed networks parsed as enums | In scope: TestPracticeInfo. Out of scope: integration with live services. |
| 47 |  | `TestPracticeInfo.test_locations_parsed` | Test that locations are parsed correctly. | Fixtures: valid_practice_info_data. Calls PracticeInfo. Asserts 3 conditions on result. | Locations parsed | In scope: TestPracticeInfo. Out of scope: integration with live services. |
| 48 |  | `TestPracticeInfo.test_transfer_numbers_parsed` | Test that transfer numbers are parsed correctly. | Fixtures: valid_practice_info_data. Calls PracticeInfo. Asserts 3 conditions on result. | Transfer numbers parsed | In scope: TestPracticeInfo. Out of scope: integration with live services. |
| 49 |  | `TestPracticeInfo.test_preflight_message_parsed` | Test that preflight_message is parsed as list of enums. | Fixtures: valid_practice_info_data. Calls PracticeInfo. Asserts 4 conditions on result. | Preflight message parsed | In scope: TestPracticeInfo. Out of scope: integration with live services. |
| 50 |  | `TestPracticeInfo.test_preflight_message_defaults_to_none` | Test that preflight_message defaults to None when not provided. | Fixtures: valid_practice_info_data. Calls PracticeInfo. Asserts info.preflight_message is None. | Preflight message defaults to none | In scope: TestPracticeInfo. Out of scope: integration with live services. |
| 51 |  | `TestPracticeInfo.test_preflight_message_empty_list` | Test that preflight_message can be an empty list. | Fixtures: valid_practice_info_data. Calls PracticeInfo. Asserts info.preflight_message == []. | Preflight message empty list | In scope: TestPracticeInfo. Out of scope: integration with live services. |
| 52 |  | `TestPracticeInfo.test_empty_lists_allowed` | Test that empty lists are valid for array fields. | Calls PracticeInfo. Asserts 2 conditions on result. | Empty lists allowed | In scope: TestPracticeInfo. Out of scope: integration with live services. |

### Suggested Missing Tests
- **Forward-compat unknown fields** - Pydantic should ignore (or fail) on unknown keys consistently.
- **Optional field defaults** - Verify each optional field has documented default.

### Improvement Suggestions / Irrelevant Tests
- **24 schema tests** - Convert to a generated table-driven test from a fixture file.

## test_preflight.py

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|---|---|---|---|---|---|
| 53 |  | `TestBuildPreflightPlaylist.test_default_when_none` | None preflight_message defaults to [recorded_info, emergency_info] + ringing. | Calls build_preflight_playlist. Asserts 5 conditions on result. | Default when none | In scope: TestBuildPreflightPlaylist. Out of scope: integration with live services. |
| 54 |  | `TestBuildPreflightPlaylist.test_default_when_empty` | Empty list defaults to [recorded_info, emergency_info] + ringing. | Calls build_preflight_playlist. Asserts 5 conditions on result. | Default when empty | In scope: TestBuildPreflightPlaylist. Out of scope: integration with live services. |
| 55 |  | `TestBuildPreflightPlaylist.test_none_sentinel_returns_empty` | 'none' in the list means skip preflight entirely. | Calls build_preflight_playlist. Asserts playlist == []. | None sentinel returns empty | In scope: TestBuildPreflightPlaylist. Out of scope: integration with live services. |
| 56 |  | `TestBuildPreflightPlaylist.test_none_sentinel_with_others_still_skips` | 'none' takes precedence even with other values. | Calls build_preflight_playlist. Asserts playlist == []. | None sentinel with others still skips | In scope: TestBuildPreflightPlaylist. Out of scope: integration with live services. |
| 57 |  | `TestBuildPreflightPlaylist.test_single_message` | Single message type produces silence + that file + ringing. | Calls build_preflight_playlist. Asserts 4 conditions on result. | Single message | In scope: TestBuildPreflightPlaylist. Out of scope: integration with live services. |
| 58 |  | `TestBuildPreflightPlaylist.test_all_messages_in_correct_order` | All message types are assembled in the canonical Zo 1.0 order, ringing last. | Calls build_preflight_playlist. Asserts names == ['preflight-silence.wav', 'preflight-record-info.wav', 'preflight-emergency-in.... | All messages in correct order | In scope: TestBuildPreflightPlaylist. Out of scope: integration with live services. |
| 59 |  | `TestBuildPreflightPlaylist.test_custom_is_ignored` | Custom message type is ignored (not supported yet). | Calls build_preflight_playlist. Asserts 4 conditions on result. | Custom is ignored | In scope: TestBuildPreflightPlaylist. Out of scope: integration with live services. |
| 60 |  | `TestBuildPreflightPlaylist.test_all_paths_are_under_assets_preflight` | All returned paths point to assets/preflight/ directory. | Calls build_preflight_playlist. Asserts 'assets/preflight' in str(path). | All paths are under assets preflight | In scope: TestBuildPreflightPlaylist. Out of scope: integration with live services. |
| 61 |  | `TestPlayPreflightAudio.test_skips_when_none_sentinel` | No track published when preflight is skipped. | Calls self._mock_room, publish_track.assert_not_called, play_preflight_audio. Verifies behavior via mocks/return value. | Skips when none sentinel [async] | In scope: TestPlayPreflightAudio. Out of scope: real LLM/network calls (mocked). |
| 62 |  | `TestPlayPreflightAudio.test_publishes_and_unpublishes_track` | Track is published before playback and unpublished after. | Calls self._mock_room, publish_track.assert_called_once_with, unpublish_track.assert_called_once_with. Verifies behavior via mocks/return value. | Publishes and unpublishes track [async] | In scope: TestPlayPreflightAudio. Out of scope: real LLM/network calls (mocked). |
| 63 |  | `TestPlayPreflightAudio.test_unpublishes_track_on_error` | Track is unpublished and source closed even when playback errors. | Calls self._mock_room, unpublish_track.assert_called_once_with, aclose.assert_called_once. Verifies behavior via mocks/return value. | Unpublishes track on error [async] | In scope: TestPlayPreflightAudio. Out of scope: real LLM/network calls (mocked). |
| 64 |  | `TestPlayPreflightAudio.test_skips_missing_wav_files` | Missing WAV files are skipped without raising. | Calls self._mock_room, wait_for_playout.assert_called_once, aclose.assert_called_once. Verifies behavior via mocks/return value. | Skips missing wav files [async] | In scope: TestPlayPreflightAudio. Out of scope: real LLM/network calls (mocked). |
| 65 |  | `TestPlayPreflightAudio.test_source_uses_16khz_sample_rate` | AudioSource matches WAV file sample rate (16kHz). | Calls self._mock_room, mock_source_cls.assert_called_once_with, patch. Verifies behavior via mocks/return value. | Source uses 16khz sample rate [async] | In scope: TestPlayPreflightAudio. Out of scope: real LLM/network calls (mocked). |
| 66 |  | `TestPlayPreflightAudio.test_closes_source_when_publish_fails` | AudioSource is closed even when publish_track raises. | Calls self._mock_room, AsyncMock, aclose.assert_called_once. Verifies behavior via mocks/return value. | Closes source when publish fails [async] | In scope: TestPlayPreflightAudio. Out of scope: real LLM/network calls (mocked). |

### Suggested Missing Tests
- **Preflight timeout** - Practice info fetch slow — preflight must time-bound.
- **Partial preflight failure** - Some checks pass, one fails — verify aggregate result.

### Improvement Suggestions / Irrelevant Tests
- **Async preflight order** - Verify checks run in parallel where possible.

## test_process_tool_response.py

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|---|---|---|---|---|---|
| 67 |  | `TestClientExtraction.test_returns_early_for_non_dict` | Returns early for non dict | Calls make_client, client._extract_and_handle_directives. Verifies behavior via mocks/return value. | Returns early for non dict [async] | In scope: TestClientExtraction. Out of scope: real LLM/network calls (mocked), no explicit assertions. |
| 68 |  | `TestClientExtraction.test_returns_early_when_no_directives_key` | Returns early when no directives key | Calls make_client, client._extract_and_handle_directives. Asserts response == {'status': 'ok'}. | Returns early when no directives key [async] | In scope: TestClientExtraction. Out of scope: integration with live services. |
| 69 |  | `TestClientExtraction.test_preserves_directives_in_response` | Preserves directives in response | Calls make_client, client._extract_and_handle_directives. Asserts 2 conditions on result. | Preserves directives in response [async] | In scope: TestClientExtraction. Out of scope: integration with live services. |
| 70 |  | `TestClientExtraction.test_invokes_handler_callback` | Invokes handler callback | Calls AsyncMock, make_client, handler.assert_called_once. Asserts 3 conditions on result. | Invokes handler callback [async] | In scope: TestClientExtraction. Out of scope: real LLM/network calls (mocked). |
| 71 |  | `TestClientExtraction.test_no_handler_logs_but_does_not_error` | No handler logs but does not error | Calls make_client, client._extract_and_handle_directives. Asserts response['directives']['speak'] == 'hello'. | No handler logs but does not error [async] | In scope: TestClientExtraction. Out of scope: integration with live services. |
| 72 |  | `TestClientExtraction.test_skips_on_parse_failure` | Skips on parse failure | Calls make_client, client._extract_and_handle_directives. Asserts response == {'directives': 'not_a_valid_structure', 'status': 'data'}. | Skips on parse failure [async] | In scope: TestClientExtraction. Out of scope: integration with live services. |
| 73 |  | `TestClientExtraction.test_empty_directives_invokes_handler_with_empty` | Empty directives invokes handler with empty | Calls AsyncMock, make_client, handler.assert_called_once. Asserts 4 conditions on result. | Empty directives invokes handler with empty [async] | In scope: TestClientExtraction. Out of scope: real LLM/network calls (mocked). |
| 74 |  | `TestClientExtraction.test_errors_preserved_in_directives` | Errors stay in directives for the LLM to inspect. | Calls AsyncMock, make_client, handler.assert_called_once. Asserts response['directives']['errors'][0]['error_description'] == 'Bad input'. | Errors preserved in directives [async] | In scope: TestClientExtraction. Out of scope: real LLM/network calls (mocked). |
| 75 |  | `TestCallEvents.test_stashes_call_events_on_dict_result` | Stashes call events on dict result | Calls make_agent, ToolDirectives, agent._handle_directives. Asserts 2 conditions on result. | Stashes call events on dict result [async] | In scope: TestCallEvents. Out of scope: integration with live services. |
| 76 |  | `TestCallEvents.test_call_events_stashed_on_empty_result` | Call events stashed on empty result | Calls make_agent, ToolDirectives, agent._handle_directives. Asserts response['__call_events'] == ['Event1']. | Call events stashed on empty result [async] | In scope: TestCallEvents. Out of scope: integration with live services. |
| 77 |  | `TestCallEvents.test_speak_and_transfer_not_handled_in_callback` | _handle_directives only handles call_events; speak/transfer are no-ops. | Calls MagicMock, AsyncMock, make_agent. Verifies behavior via mocks/return value. | Speak and transfer not handled in callback [async] | In scope: TestCallEvents. Out of scope: real LLM/network calls (mocked). |
| 78 |  | `TestExtractDirectives.test_empty_event` | Empty event | Fixtures: extract. Calls _make_tool_event, extract. Asserts 4 conditions on result. | Empty event | In scope: TestExtractDirectives. Out of scope: integration with live services. |
| 79 |  | `TestExtractDirectives.test_top_level_speak` | Top level speak | Fixtures: extract. Calls _make_tool_event, extract, json.dumps. Asserts 2 conditions on result. | Top level speak | In scope: TestExtractDirectives. Out of scope: integration with live services. |
| 80 |  | `TestExtractDirectives.test_top_level_speak_with_routing` | Top level speak with routing | Fixtures: extract. Calls _make_tool_event, extract, json.dumps. Asserts 2 conditions on result. | Top level speak with routing | In scope: TestExtractDirectives. Out of scope: integration with live services. |
| 81 |  | `TestExtractDirectives.test_envelope_speak` | Envelope speak | Fixtures: extract. Calls _make_tool_event, extract, json.dumps. Asserts batch.speak == 'Server says hi'. | Envelope speak | In scope: TestExtractDirectives. Out of scope: integration with live services. |
| 82 |  | `TestExtractDirectives.test_top_level_speak_takes_priority_over_envelope` | When both top-level and envelope speak exist, top-level wins. | Fixtures: extract. Calls _make_tool_event, extract, json.dumps. Asserts batch.speak == 'Agent says'. | Top level speak takes priority over envelope | In scope: TestExtractDirectives. Out of scope: integration with live services. |
| 83 |  | `TestExtractDirectives.test_envelope_transfer` | Envelope transfer | Fixtures: extract. Calls _make_tool_event, extract, json.dumps. Asserts 4 conditions on result. | Envelope transfer | In scope: TestExtractDirectives. Out of scope: integration with live services. |
| 84 |  | `TestExtractDirectives.test_multiple_tool_outputs_last_speak_wins` | When multiple tools return speak, the last one is used. | Fixtures: extract. Calls _make_tool_event, extract, json.dumps. Asserts 2 conditions on result. | Multiple tool outputs last speak wins | In scope: TestExtractDirectives. Out of scope: integration with live services. |
| 85 |  | `TestExtractDirectives.test_skips_none_and_error_outputs` | Skips none and error outputs | Fixtures: extract. Calls MagicMock, json.dumps, extract. Asserts 2 conditions on result. | Skips none and error outputs | In scope: TestExtractDirectives. Out of scope: real LLM/network calls (mocked). |
| 86 |  | `TestExtractDirectives.test_skips_non_json_output` | Skips non json output | Fixtures: extract. Calls _make_tool_event, extract. Asserts batch.speak is None. | Skips non json output | In scope: TestExtractDirectives. Out of scope: integration with live services. |
| 87 |  | `TestExtractDirectives.test_skips_non_dict_json` | Skips non dict json | Fixtures: extract. Calls _make_tool_event, extract, json.dumps. Asserts batch.speak is None. | Skips non dict json | In scope: TestExtractDirectives. Out of scope: integration with live services. |
| 88 |  | `TestExtractDirectives.test_no_directives_in_output` | No directives in output | Fixtures: extract. Calls _make_tool_event, extract, json.dumps. Asserts not batch.has_work. | No directives in output | In scope: TestExtractDirectives. Out of scope: integration with live services. |
| 89 |  | `TestExtractDirectives.test_top_level_speak_stripped_from_output` | Anti-echo: speak and routing_instructions are removed from tool output. | Fixtures: extract. Calls _make_tool_event, extract, json.loads. Asserts 3 conditions on result. | Top level speak stripped from output | In scope: TestExtractDirectives. Out of scope: integration with live services. |
| 90 |  | `TestExtractDirectives.test_envelope_speak_stripped_from_output` | Anti-echo: speak is removed from directives envelope in tool output. | Fixtures: extract. Calls _make_tool_event, extract, json.loads. Asserts 2 conditions on result. | Envelope speak stripped from output | In scope: TestExtractDirectives. Out of scope: integration with live services. |
| 91 |  | `TestProcessDirectives.test_delivers_speech_and_injects_context` | Delivers speech and injects context | Calls MagicMock, AsyncMock, make_agent. Asserts 2 conditions on result. | Delivers speech and injects context [async] | In scope: TestProcessDirectives. Out of scope: real LLM/network calls (mocked). |
| 92 |  | `TestProcessDirectives.test_routing_instructions_triggers_generate_reply` | Routing instructions triggers generate reply | Calls MagicMock, AsyncMock, make_agent. Verifies behavior via mocks/return value. | Routing instructions triggers generate reply [async] | In scope: TestProcessDirectives. Out of scope: real LLM/network calls (mocked). |
| 93 |  | `TestProcessDirectives.test_transfer_calls_execute_transfer` | Transfer calls execute transfer | Calls MagicMock, make_agent, make_handler. Verifies behavior via mocks/return value. | Transfer calls execute transfer [async] | In scope: TestProcessDirectives. Out of scope: real LLM/network calls (mocked). |
| 94 |  | `TestProcessDirectives.test_transfer_defaults_to_catch_all` | Transfer defaults to catch all | Calls MagicMock, make_agent, make_handler. Asserts call_kwargs['transfer_number_type'] == TransferNumberType.CATCH_ALL. | Transfer defaults to catch all [async] | In scope: TestProcessDirectives. Out of scope: real LLM/network calls (mocked). |
| 95 |  | `TestProcessDirectives.test_speak_then_transfer_ordering` | Speech is delivered before transfer is executed. | Calls MagicMock, make_agent, make_handler. Asserts call_order == ['speak', 'transfer']. | Speak then transfer ordering [async] | In scope: TestProcessDirectives. Out of scope: real LLM/network calls (mocked). |
| 96 |  | `TestProcessDirectives.test_no_work_does_nothing` | No work does nothing | Calls MagicMock, AsyncMock, make_agent. Verifies behavior via mocks/return value. | No work does nothing [async] | In scope: TestProcessDirectives. Out of scope: real LLM/network calls (mocked). |
| 97 |  | `TestAllowInlineAgentTask.test_sets_inline_task_on_current_task` | Marks the current asyncio.Task so LiveKit allows AgentTask to run. | Calls make_handler, handler._allow_inline_agent_task, asyncio.current_task. Asserts 2 conditions on result. | Sets inline task on current task [async] | In scope: TestAllowInlineAgentTask. Out of scope: integration with live services. |
| 98 |  | `TestAllowInlineAgentTask.test_preserves_existing_task_info` | If the attribute already exists, only flips inline_task. | Calls make_handler, asyncio.current_task, _set_activity_task_info. Asserts 2 conditions on result. | Preserves existing task info [async] | In scope: TestAllowInlineAgentTask. Out of scope: integration with live services. |
| 99 |  | `TestExecuteConfirmableCommit.test_commit_errors_trigger_generate_reply` | When commit returns directives.errors, the LLM is told to recover. | Calls MagicMock, AsyncMock, make_agent. Asserts 3 conditions on result. | Commit errors trigger generate reply [async] | In scope: TestExecuteConfirmableCommit. Out of scope: real LLM/network calls (mocked). |
| 100 |  | `TestExecuteConfirmableCommit.test_top_level_error_triggers_system_transfer` | When commit returns a top-level error (static tool exception handler), the caller is transferred to a human. | Calls MagicMock, make_agent, AsyncMock. Verifies behavior via mocks/return value. | Top level error triggers system transfer [async] | In scope: TestExecuteConfirmableCommit. Out of scope: real LLM/network calls (mocked). |
| 101 |  | `TestExecuteConfirmableCommit.test_invoke_tool_exception_emits_hard_break_metric_and_event` | Regression: when the commit invocation raises (e.g. | Calls _make_session_with_userdata, make_agent, AsyncMock. Asserts 2 conditions on result. | Invoke tool exception emits hard break metric and event [async] | In scope: TestExecuteConfirmableCommit. Out of scope: real LLM/network calls (mocked). |
| 102 |  | `TestExecuteConfirmableCommit.test_commit_success_speaks_and_routes` | When commit returns directives.speak, success speech is delivered. | Calls MagicMock, AsyncMock, make_agent. Asserts 'committed successfully' in instructions. | Commit success speaks and routes [async] | In scope: TestExecuteConfirmableCommit. Out of scope: real LLM/network calls (mocked). |
| 103 |  | `TestOnFunctionToolsExecuted.test_cancel_tool_reply_on_speech` | cancel_tool_reply is called when batch has speak. | Calls make_handler, _make_tool_event, handler.handle_event. Verifies behavior via mocks/return value. | Cancel tool reply on speech [async] | In scope: TestOnFunctionToolsExecuted. Out of scope: real LLM/network calls (mocked). |
| 104 |  | `TestOnFunctionToolsExecuted.test_cancel_tool_reply_on_transfer` | cancel_tool_reply is called when batch has a transfer directive, even without any speech — prevents the LLM from generating a reply that conflicts with the transfer (e.g., 'successfully rescheduled'... | Calls make_handler, _make_tool_event, handler.handle_event. Verifies behavior via mocks/return value. | Cancel tool reply on transfer [async] | In scope: TestOnFunctionToolsExecuted. Out of scope: real LLM/network calls (mocked). |
| 105 |  | `TestOnFunctionToolsExecuted.test_no_cancel_when_no_work` | cancel_tool_reply is NOT called when batch has no directives. | Calls make_handler, _make_tool_event, handler.handle_event. Verifies behavior via mocks/return value. | No cancel when no work [async] | In scope: TestOnFunctionToolsExecuted. Out of scope: real LLM/network calls (mocked). |
| 106 |  | `TestEndToEnd.test_client_extracts_and_callback_stashes_call_events` | Callback handles call_events; speak/transfer are left for event handler. | Calls MagicMock, AsyncMock, make_agent. Asserts 3 conditions on result. | Client extracts and callback stashes call events [async] | In scope: TestEndToEnd. Out of scope: real LLM/network calls (mocked). |
| 107 |  | `TestEndToEnd.test_errors_remain_in_response_for_llm` | Errors stay in directives for the LLM to inspect. | Calls make_agent, make_client, client._extract_and_handle_directives. Asserts 2 conditions on result. | Errors remain in response for llm [async] | In scope: TestEndToEnd. Out of scope: integration with live services. |
| 108 |  | `TestRunConfirmationFlowSubmittedTimeslot.test_yes_for_reschedule_publishes_submitted_timeslot` | Yes for reschedule publishes submitted timeslot | Calls _make_confirmation_agent, make_handler, AsyncMock. Asserts len(timeslot_events) == 1. | Yes for reschedule publishes submitted timeslot [async] | In scope: TestRunConfirmationFlowSubmittedTimeslot. Out of scope: real LLM/network calls (mocked). |
| 109 |  | `TestRunConfirmationFlowSubmittedTimeslot.test_yes_for_non_timeslot_tool_does_not_publish` | Other confirmable tools (e.g. | Calls _make_confirmation_agent, make_handler, AsyncMock. Asserts timeslot_events == []. | Yes for non timeslot tool does not publish [async] | In scope: TestRunConfirmationFlowSubmittedTimeslot. Out of scope: real LLM/network calls (mocked). |
| 110 |  | `TestRunConfirmationFlowSubmittedTimeslot.test_no_does_not_publish` | If the caller says NO, there is no submission, so no event. | Calls _make_confirmation_agent, make_handler, AsyncMock. Asserts published == []. | No does not publish [async] | In scope: TestRunConfirmationFlowSubmittedTimeslot. Out of scope: real LLM/network calls (mocked). |
| 111 |  | `TestResolveNextPendingTask.test_returns_none_when_demo_bundle_disabled` | Returns none when demo bundle disabled | Calls _make_pending_task_handler, patch, handler._resolve_next_pending_task. Asserts result is None. | Returns none when demo bundle disabled | In scope: TestResolveNextPendingTask. Out of scope: real LLM/network calls (mocked). |
| 112 |  | `TestResolveNextPendingTask.test_returns_none_when_caller_not_in_allowlist` | Returns none when caller not in allowlist | Calls _make_pending_task_handler, patch, handler._resolve_next_pending_task. Asserts result is None. | Returns none when caller not in allowlist | In scope: TestResolveNextPendingTask. Out of scope: real LLM/network calls (mocked). |
| 113 |  | `TestResolveNextPendingTask.test_returns_none_when_no_pending_tasks` | Returns none when no pending tasks | Calls _make_pending_task_handler, patch, handler._resolve_next_pending_task. Asserts result is None. | Returns none when no pending tasks | In scope: TestResolveNextPendingTask. Out of scope: real LLM/network calls (mocked). |
| 114 |  | `TestResolveNextPendingTask.test_removes_completed_task_and_returns_next` | Removes completed task and returns next | Calls _make_appointment, _make_pending_task_handler, patch. Asserts 5 conditions on result. | Removes completed task and returns next | In scope: TestResolveNextPendingTask. Out of scope: real LLM/network calls (mocked). |
| 115 |  | `TestResolveNextPendingTask.test_returns_none_when_all_tasks_resolved` | Returns none when all tasks resolved | Calls _make_pending_task_handler, patch, handler._resolve_next_pending_task. Asserts 2 conditions on result. | Returns none when all tasks resolved | In scope: TestResolveNextPendingTask. Out of scope: real LLM/network calls (mocked). |
| 116 |  | `TestResolveNextPendingTask.test_unique_appointment_match_produces_specific_directive` | Unique appointment match produces specific directive | Calls _make_appointment, _make_pending_task_handler, patch. Asserts 4 conditions on result. | Unique appointment match produces specific directive | In scope: TestResolveNextPendingTask. Out of scope: real LLM/network calls (mocked). |
| 117 |  | `TestResolveNextPendingTask.test_no_appointment_match_produces_generic_directive` | No appointment match produces generic directive | Calls _make_appointment, _make_pending_task_handler, patch. Asserts 3 conditions on result. | No appointment match produces generic directive | In scope: TestResolveNextPendingTask. Out of scope: real LLM/network calls (mocked). |
| 118 |  | `TestResolveNextPendingTask.test_uses_fetched_appointments_over_session_state` | Uses fetched appointments over session state | Calls _make_appointment, _make_pending_task_handler, patch. Asserts 3 conditions on result. | Uses fetched appointments over session state | In scope: TestResolveNextPendingTask. Out of scope: real LLM/network calls (mocked). |
| 119 |  | `TestResolveNextPendingTask.test_filters_non_string_pending_tasks` | Filters non string pending tasks | Calls _make_appointment, _make_pending_task_handler, patch. Asserts 2 conditions on result. | Filters non string pending tasks | In scope: TestResolveNextPendingTask. Out of scope: real LLM/network calls (mocked). |

### Suggested Missing Tests
- **Malformed tool response (not JSON)** - Tool returns plain text — verify graceful handling.
- **Tool response > token budget** - Truncate vs error.
- **Tool response with PHI** - Verify redaction before logging.

### Improvement Suggestions / Irrelevant Tests
- **22 tests, repetitive** - Parametrize the response-shape matrix.

## test_prompt_loader.py

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|---|---|---|---|---|---|
| 120 |  | `TestStructuredFormat.test_loads_structured_format` | Structured format (has type: structured) assembles sections. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts 7 conditions on result. | Loads structured format | In scope: TestStructuredFormat. Out of scope: real LLM/network calls (mocked). |
| 121 |  | `TestStructuredFormat.test_section_ordering` | Sections appear in priority order. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts identity_pos < context_pos < instructions_pos < rules_pos. | Section ordering | In scope: TestStructuredFormat. Out of scope: real LLM/network calls (mocked). |
| 122 |  | `TestStructuredFormat.test_practice_name_in_context_block` | Practice name appears in context block, not inline. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts 2 conditions on result. | Practice name in context block | In scope: TestStructuredFormat. Out of scope: real LLM/network calls (mocked). |
| 123 |  | `TestStructuredFormat.test_optional_sections_omitted` | Optional sections are omitted if not provided. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts 3 conditions on result. | Optional sections omitted | In scope: TestStructuredFormat. Out of scope: real LLM/network calls (mocked). |
| 124 |  | `TestStructuredFormat.test_handoff_guidance_appended` | Handoff guidance appears at the end. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts handoff_pos > rules_pos. | Handoff guidance appended | In scope: TestStructuredFormat. Out of scope: real LLM/network calls (mocked). |
| 125 |  | `TestPracticeNameVariations.test_prompt_structure_stable` | Prompt structure remains consistent regardless of practice name length. | Fixtures: practice_name, tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts 6 conditions on result. | Prompt structure stable (parametrized) | In scope: TestPracticeNameVariations. Out of scope: real LLM/network calls (mocked). |
| 126 |  | `TestSharedPromptSections.test_shared_identity_prepended` | Shared identity is prepended before agent identity. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts 3 conditions on result. | Shared identity prepended | In scope: TestSharedPromptSections. Out of scope: real LLM/network calls (mocked). |
| 127 |  | `TestSharedPromptSections.test_shared_rules_prepended` | Shared rules appear before agent-specific rules. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts 4 conditions on result. | Shared rules prepended | In scope: TestSharedPromptSections. Out of scope: real LLM/network calls (mocked). |
| 128 |  | `TestSharedPromptSections.test_shared_rules_with_no_agent_rules` | Shared rules appear even when agent has no rules. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts 3 conditions on result. | Shared rules with no agent rules | In scope: TestSharedPromptSections. Out of scope: real LLM/network calls (mocked). |
| 129 |  | `TestSharedPromptSections.test_missing_shared_yaml_raises` | Missing _shared.yaml hard-fails for structured prompts. | Fixtures: tmp_path, monkeypatch. Calls prompts_dir.mkdir, , monkeypatch.setattr. Verifies behavior via mocks/return value. | Missing shared yaml raises | In scope: TestSharedPromptSections. Out of scope: real LLM/network calls (mocked), no explicit assertions. |
| 130 |  | `TestSharedPromptSections.test_shared_instructions_prepended` | Shared instructions are prepended before agent instructions. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts 3 conditions on result. | Shared instructions prepended | In scope: TestSharedPromptSections. Out of scope: real LLM/network calls (mocked). |
| 131 |  | `TestSharedPromptSections.test_empty_shared_identity` | Empty shared identity doesn't add extra whitespace. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts 2 conditions on result. | Empty shared identity | In scope: TestSharedPromptSections. Out of scope: real LLM/network calls (mocked). |
| 132 |  | `test_load_prompt_raises_when_type_not_structured` | Test that load_prompt rejects prompts without type: structured. | Fixtures: tmp_path, monkeypatch. Calls _write_agent_yaml, monkeypatch.setattr, pytest.raises. Verifies behavior via mocks/return value. | Load prompt raises when type not structured | In scope: unit-level behavior. Out of scope: real LLM/network calls (mocked), no explicit assertions. |
| 133 |  | `test_load_agent_description_returns_value` | Test that load_agent_description returns YAML description metadata. | Fixtures: tmp_path, monkeypatch. Calls _write_agent_yaml, monkeypatch.setattr, load_agent_description. Asserts load_agent_description('test_agent') == 'test handoff description'. | Load agent description returns value | In scope: unit-level behavior. Out of scope: real LLM/network calls (mocked). |
| 134 |  | `test_load_agent_description_raises_when_missing_field` | Test that load_agent_description raises when field is absent. | Fixtures: tmp_path, monkeypatch. Calls _write_agent_yaml, monkeypatch.setattr, pytest.raises. Verifies behavior via mocks/return value. | Load agent description raises when missing field | In scope: unit-level behavior. Out of scope: real LLM/network calls (mocked), no explicit assertions. |
| 135 |  | `test_load_agent_description_raises_when_file_missing` | Test that load_agent_description raises for missing prompt file. | Fixtures: tmp_path, monkeypatch. Calls prompts_dir.mkdir, monkeypatch.setattr, pytest.raises. Verifies behavior via mocks/return value. | Load agent description raises when file missing | In scope: unit-level behavior. Out of scope: real LLM/network calls (mocked), no explicit assertions. |
| 136 |  | `test_load_prompt_populates_placeholder_session_state_fields` | Test only allowlisted SessionState placeholders are substituted. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts 'Hello Test Medical Group {{first_name}} {{last_name}} {{non_existent_field}}!' in result. | Load prompt populates placeholder session state fields | In scope: unit-level behavior. Out of scope: real LLM/network calls (mocked). |
| 137 |  | `test_structured_format_raises_when_instructions_missing` | Test that structured format requires instructions field. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Verifies behavior via mocks/return value. | Structured format raises when instructions missing | In scope: unit-level behavior. Out of scope: real LLM/network calls (mocked), no explicit assertions. |
| 138 |  | `test_unknown_type_raises_error` | Test that unknown type value raises clear error. | Fixtures: tmp_path, monkeypatch. Calls _write_agent_yaml, monkeypatch.setattr, pytest.raises. Verifies behavior via mocks/return value. | Unknown type raises error | In scope: unit-level behavior. Out of scope: real LLM/network calls (mocked), no explicit assertions. |
| 139 |  | `test_none_kwargs_do_not_raise_attribute_error` | Test that passing None for optional kwargs doesn't cause AttributeError. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts 3 conditions on result. | None kwargs do not raise attribute error | In scope: unit-level behavior. Out of scope: real LLM/network calls (mocked). |
| 140 |  | `test_authenticated_patient_context_with_partial_names` | Test that authenticated patient context handles missing name fields gracefully. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts 6 conditions on result. | Authenticated patient context with partial names | In scope: unit-level behavior. Out of scope: real LLM/network calls (mocked). |
| 141 |  | `TestRealPromptPlaceholders.test_confirm_full_name_task_substitutes_names` | confirm-full-name-task substitutes first_name and last_name kwargs. | Calls load_prompt. Asserts 3 conditions on result. | Confirm full name task substitutes names | In scope: TestRealPromptPlaceholders. Out of scope: integration with live services. |
| 142 |  | `TestRealPromptPlaceholders.test_confirm_full_name_task_leaves_unresolved_without_kwargs` | Without kwargs, placeholders remain as-is (not an error). | Calls load_prompt. Asserts 2 conditions on result. | Confirm full name task leaves unresolved without kwargs | In scope: TestRealPromptPlaceholders. Out of scope: integration with live services. |
| 143 |  | `TestRealPromptPlaceholders.test_select_transfer_location_task_substitutes_locations` | select-transfer-location-task substitutes locations_text kwarg. | Calls load_prompt. Asserts 3 conditions on result. | Select transfer location task substitutes locations | In scope: TestRealPromptPlaceholders. Out of scope: integration with live services. |
| 144 |  | `TestSharedTimePlaceholders.test_time_placeholders_resolved_with_session_state` | All prompts must resolve shared time placeholders when session_state is provided. | Fixtures: prompt_name. Calls make_session_state, load_prompt. Asserts 3 conditions on result. | Time placeholders resolved with session state (parametrized) | In scope: TestSharedTimePlaceholders. Out of scope: integration with live services. |
| 145 |  | `TestTaskNameRequired.test_task_defines_task_name` | Tasks with YAML prompts must define task_name for session_state rendering. | Fixtures: task_class_path, expected_task_name. Calls task_class_path.rsplit, importlib.import_module, task_cls.__new__. Asserts 2 conditions on result. | Task defines task name (parametrized) | In scope: TestTaskNameRequired. Out of scope: integration with live services. |
| 146 |  | `TestPlaceholderErrorLogging.test_no_error_logged_when_disabled` | When log_missing_placeholders=False, missing placeholders should NOT log an error. | Fixtures: caplog. Calls caplog.at_level, load_prompt. Asserts 2 conditions on result. | No error logged when disabled | In scope: TestPlaceholderErrorLogging. Out of scope: integration with live services. |
| 147 |  | `TestPlaceholderErrorLogging.test_error_logged_by_default` | By default, missing placeholders log an error. | Fixtures: caplog. Calls make_session_state, caplog.at_level, load_prompt. Asserts 2 conditions on result. | Error logged by default | In scope: TestPlaceholderErrorLogging. Out of scope: integration with live services. |
| 148 |  | `TestVoiceQualityV2Rules.test_v2_rules_excluded_when_flag_off` | V2 rules are NOT in the prompt when use_voice_quality_v2 is False. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts 2 conditions on result. | V2 rules excluded when flag off | In scope: TestVoiceQualityV2Rules. Out of scope: real LLM/network calls (mocked). |
| 149 |  | `TestVoiceQualityV2Rules.test_v2_rules_included_when_flag_on` | V2 rules ARE in the prompt when use_voice_quality_v2 is True. | Fixtures: tmp_path, monkeypatch. Calls _write_shared_yaml, _write_agent_yaml, monkeypatch.setattr. Asserts 2 conditions on result. | V2 rules included when flag on | In scope: TestVoiceQualityV2Rules. Out of scope: real LLM/network calls (mocked). |

### Suggested Missing Tests
- **Circular include** - Prompt A includes B includes A — should fail with clear error.
- **Variable not provided** - Prompt references `{x}` but no `x` — current behavior?

### Improvement Suggestions / Irrelevant Tests
- **Heavy fixture YAMLs** - Many tests build fixture files in `tmp_path`; extract a builder helper.

## test_tool_endpoint_execution.py

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|---|---|---|---|---|---|
| 150 |  | `TestToolEndpointExecution.test_call_tool_endpoint_get_with_path_params` | Test GET request with path parameter substitution. | Fixtures: get_tool_def, mock_prompt_loader. Calls TestAgent, patch, Mock. Asserts 6 conditions on result. | Call tool endpoint get with path params [async] | In scope: TestToolEndpointExecution. Out of scope: real LLM/network calls (mocked). |
| 151 |  | `TestToolEndpointExecution.test_call_tool_endpoint_post_with_json_body` | Test POST request with JSON body. | Fixtures: post_tool_def, mock_prompt_loader. Calls TestAgent, patch, Mock. Asserts 4 conditions on result. | Call tool endpoint post with json body [async] | In scope: TestToolEndpointExecution. Out of scope: real LLM/network calls (mocked). |
| 152 |  | `TestToolEndpointExecution.test_call_tool_endpoint_get_with_query_params` | Test GET request with query parameters (no path params). | Fixtures: get_query_tool_def, mock_prompt_loader. Calls TestAgent, patch, Mock. Asserts 4 conditions on result. | Call tool endpoint get with query params [async] | In scope: TestToolEndpointExecution. Out of scope: real LLM/network calls (mocked). |
| 153 |  | `TestToolEndpointExecution.test_call_tool_endpoint_session_path_param_filled_when_llm_omits` | Deterministic path placeholders (:practice_phone_number, :caller_phone_number) must be filled from session even when the LLM does not pass them as arguments. | Fixtures: mock_prompt_loader. Calls ToolDefinition, TestAgent, patch. Asserts 2 conditions on result. | Call tool endpoint session path param filled when llm omits [async] | In scope: TestToolEndpointExecution. Out of scope: real LLM/network calls (mocked). |
| 154 |  | `TestToolEndpointExecution.test_call_tool_endpoint_error_propagation` | Test that API errors are propagated correctly. | Fixtures: get_tool_def, mock_prompt_loader. Calls TestAgent, patch, Mock. Asserts exc_info.value.status_code == 404. | Call tool endpoint error propagation [async] | In scope: TestToolEndpointExecution. Out of scope: real LLM/network calls (mocked). |
| 155 |  | `TestToolEndpointExecution.test_call_tool_endpoint_client_cleanup` | Test that client cleanup is called even on success. | Fixtures: get_tool_def, mock_prompt_loader. Calls TestAgent, patch, Mock. Verifies behavior via mocks/return value. | Call tool endpoint client cleanup [async] | In scope: TestToolEndpointExecution. Out of scope: real LLM/network calls (mocked). |
| 156 |  | `TestToolEndpointExecution.test_call_tool_endpoint_mixed_path_and_query_params` | Test GET with both path params and remaining params as query. | Fixtures: mock_prompt_loader. Calls ToolDefinition, TestAgent, patch. Asserts 4 conditions on result. | Call tool endpoint mixed path and query params [async] | In scope: TestToolEndpointExecution. Out of scope: real LLM/network calls (mocked). |
| 157 |  | `TestToolEndpointExecution.test_call_tool_endpoint_passes_session_headers` | Test that session headers are passed to ZoToolsClient constructor. | Fixtures: get_tool_def, mock_prompt_loader. Calls TestAgent, patch, Mock. Verifies behavior via mocks/return value. | Call tool endpoint passes session headers [async] | In scope: TestToolEndpointExecution. Out of scope: real LLM/network calls (mocked). |
| 158 |  | `TestToolEndpointExecution.test_call_tool_endpoint_overrides_phone_path_params_from_session` | Session state phone numbers override LLM-provided path params. | Fixtures: mock_prompt_loader, path_param_name, llm_value, expected_value, session_header_key. Calls ToolDefinition, TestAgent, patch. Asserts call_args[1]['path_params'] == {path_param_name: expected_value}. | Call tool endpoint overrides phone path params from session (parametrized) [async] | In scope: TestToolEndpointExecution. Out of scope: real LLM/network calls (mocked). |
| 159 |  | `TestDynamicToolExecutor400Handling.test_400_returns_error_dict_to_llm` | A 400 from zo-tools is returned as a dict, not re-raised. | Fixtures: post_tool_def, mock_prompt_loader. Calls TestAgent, _make_run_context, patch.object. Asserts result == {'success': False, 'error': error_body}. | 400 returns error dict to llm [async] | In scope: TestDynamicToolExecutor400Handling. Out of scope: real LLM/network calls (mocked). |
| 160 |  | `TestDynamicToolExecutor400Handling.test_non_400_api_error_still_raises` | Non-400 ApiResponseErrors (e.g. | Fixtures: post_tool_def, mock_prompt_loader. Calls TestAgent, _make_run_context, patch.object. Asserts exc_info.value.status_code == 500. | Non 400 api error still raises [async] | In scope: TestDynamicToolExecutor400Handling. Out of scope: real LLM/network calls (mocked). |
| 161 |  | `TestDynamicToolExecutor400Handling.test_non_api_error_still_raises` | Non-ApiResponseError exceptions still re-raise for auto-transfer. | Fixtures: post_tool_def, mock_prompt_loader. Calls TestAgent, _make_run_context, patch.object. Verifies behavior via mocks/return value. | Non api error still raises [async] | In scope: TestDynamicToolExecutor400Handling. Out of scope: real LLM/network calls (mocked), no explicit assertions. |

### Suggested Missing Tests
- **Setup-only file** - No tests detected — tool endpoint execution is the critical seam.

### Improvement Suggestions / Irrelevant Tests
- **Empty file** - No coverage.

## test_tool_schema.py

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|---|---|---|---|---|---|
| 162 |  | `test_tool_definition_model` | Test ToolDefinition with complete data. | Calls ToolDefinition. Asserts 3 conditions on result. | Tool definition model | In scope: unit-level behavior. Out of scope: integration with live services. |
| 163 |  | `test_agent_tool_schema_model` | Test complete AgentToolSchema with sample data. | Calls AgentToolSchema. Asserts 4 conditions on result. | Agent tool schema model | In scope: unit-level behavior. Out of scope: integration with live services. |
| 164 |  | `test_agent_tool_schema_validation_missing_agent` | Test validation fails when agent field is missing. | Calls pytest.raises, AgentToolSchema. Asserts 'agent' in str(exc_info.value). | Agent tool schema validation missing agent | In scope: unit-level behavior. Out of scope: integration with live services. |
| 165 |  | `test_tool_definition_validation_invalid_method` | Test validation fails for invalid HTTP method. | Calls pytest.raises, ToolDefinition. Verifies behavior via mocks/return value. | Tool definition validation invalid method | In scope: unit-level behavior. Out of scope: real LLM/network calls (mocked), no explicit assertions. |
| 166 |  | `test_tool_definition_empty_tools_list` | Test agent schema allows empty tools list. | Calls AgentToolSchema. Asserts schema.tools == []. | Tool definition empty tools list | In scope: unit-level behavior. Out of scope: integration with live services. |
| 167 |  | `TestToolResponse.test_parse_response_without_directives` | Parse response without directives | Calls ToolResponse.model_validate. Asserts response.directives is None. | Parse response without directives | In scope: TestToolResponse. Out of scope: integration with live services. |
| 168 |  | `TestToolResponse.test_parse_response_with_all_directives` | Parse response with all directives | Calls ToolResponse.model_validate. Asserts 5 conditions on result. | Parse response with all directives | In scope: TestToolResponse. Out of scope: integration with live services. |
| 169 |  | `TestToolResponse.test_parse_response_with_partial_directives` | Parse response with partial directives | Calls ToolResponse.model_validate. Asserts 3 conditions on result. | Parse response with partial directives | In scope: TestToolResponse. Out of scope: integration with live services. |
| 170 |  | `TestToolResponse.test_parse_response_with_empty_directives` | Parse response with empty directives | Calls ToolResponse.model_validate. Asserts 3 conditions on result. | Parse response with empty directives | In scope: TestToolResponse. Out of scope: integration with live services. |
| 171 |  | `TestToolResponse.test_parse_response_with_null_directives` | Parse response with null directives | Calls ToolResponse.model_validate. Asserts response.directives is None. | Parse response with null directives | In scope: TestToolResponse. Out of scope: integration with live services. |
| 172 |  | `TestToolResponse.test_extra_fields_allowed` | ToolResponse accepts arbitrary tool-specific fields via extra='allow'. | Calls ToolResponse.model_validate. Asserts response.directives.speak == 'hi'. | Extra fields allowed | In scope: TestToolResponse. Out of scope: integration with live services. |

### Suggested Missing Tests
- **Schema drift** - Snapshot the JSON schema sent to the LLM; flag breaking changes.
- **Optional vs required** - Verify required args reflect business contract.

### Improvement Suggestions / Irrelevant Tests
- **Inline expected schemas** - Move expected JSON to fixture files for diff readability.
